package org.example.lesson1.DB;

public class Task1 {
    public static void sort (int[] array){
        for (i = array.length/2 -1 ; i >= 0; i--)
            heapify(array,array.length,i);
        for (int i = array; i < ; i++) {

        }
    }
}
